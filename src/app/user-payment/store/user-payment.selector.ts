// import { createFeatureSelector } from "@ngrx/store";
// import { AccountState,PaymentState,State } from "../user-payment.model";
// import { featureKey } from "./user-payment.reducer";

 export const FEATURE_NAME = 'account';

// export const selectAccountState = createFeatureSelector<State, AccountState>(
//   FEATURE_NAME
// );

// export interface AppState {
//     todosSliceState: PaymentState;
//   }
  
//   export const selectTodosSliceState = createFeatureSelector<AppState, PaymentState>(featureKey);